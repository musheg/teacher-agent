[project]
name = "teacher-agents"
version = "0.1.0"
description = "Teacher Agents API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "python-multipart>=0.0.20",
    "uvicorn[standard]>=0.41.0",
]
